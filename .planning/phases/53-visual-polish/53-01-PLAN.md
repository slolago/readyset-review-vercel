---
phase: 53-visual-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/Modal.tsx
  - src/app/api/folders/[folderId]/preview-assets/route.ts
  - src/components/files/FolderBrowser.tsx
  - src/components/files/AssetCard.tsx
  - src/components/review/CreateReviewLinkModal.tsx
  - src/app/(app)/dashboard/page.tsx
autonomous: false
requirements:
  - VIS-01
  - VIS-02
  - VIS-03
  - VIS-04
  - VIS-05
  - VIS-06
  - VIS-07
  - VIS-08
must_haves:
  truths:
    - "New Folder modal's top accent line is visually clipped to the rounded card — nothing protrudes at the corners"
    - "A folder with assets shows 1-4 thumbnails tiled inside its card; an empty folder still shows the folder icon"
    - "Inline-renaming a folder or an asset shows a checkmark (commit) and X (cancel) button; blur no longer commits"
    - "Asset card thumbnails show full image/video frame via object-contain within a fixed-height frame"
    - "Each asset card shows the version badge exactly once (overlay only — no duplicate label in info row)"
    - "Tag / badge pills over thumbnails stay readable on bright images (dark translucent bg + light text)"
    - "All Create Review Link modal controls stay inside the modal card's rounded border at md size"
    - "Dashboard Browse Projects / Upload Assets / Invite Team each navigate to distinct destinations"
  artifacts:
    - path: "src/app/api/folders/[folderId]/preview-assets/route.ts"
      provides: "GET endpoint returning up to 4 most recent assets with thumbnailSignedUrl/signedUrl for folder preview tiles"
      exports: ["GET"]
    - path: "src/components/ui/Modal.tsx"
      provides: "Modal with accent line clipped to card edge (overflow-hidden on card)"
    - path: "src/components/files/FolderBrowser.tsx"
      provides: "FolderCard with thumbnail preview tiles, rename confirm buttons (check/X), blur does not commit"
    - path: "src/components/files/AssetCard.tsx"
      provides: "Aspect-preserving thumbnail (object-contain), single version badge, tag contrast, rename confirm buttons"
    - path: "src/components/review/CreateReviewLinkModal.tsx"
      provides: "All controls contained within modal card"
    - path: "src/app/(app)/dashboard/page.tsx"
      provides: "QuickActions with distinct targets for Browse/Upload/Invite"
  key_links:
    - from: "src/components/files/FolderBrowser.tsx (FolderCard)"
      to: "/api/folders/[folderId]/preview-assets"
      via: "fetch in useEffect on mount"
      pattern: "preview-assets"
    - from: "src/components/ui/Modal.tsx"
      to: "card container"
      via: "overflow-hidden class so accent gradient line is clipped by rounded corners"
      pattern: "overflow-hidden"
---

<objective>
Close 8 visible UI-polish bugs in v1.7 QA. Each VIS maps to one atomic task that can be committed individually. Surgical edits only — no redesign, no new features outside the stated fixes.

Purpose: Make the grid, modals, folder cards, and dashboard read consistently before milestone v1.8 ships.
Output: Modal accent clipped, folder preview API + tiled thumbnails, explicit rename confirm affordance, aspect-preserving asset thumbnails, single version badge, legible tag pills, contained review-link modal, distinct Quick Action routes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/53-visual-polish/53-CONTEXT.md

@src/components/ui/Modal.tsx
@src/components/files/CreateFolderModal.tsx
@src/components/files/FolderBrowser.tsx
@src/components/files/AssetCard.tsx
@src/components/review/CreateReviewLinkModal.tsx
@src/app/(app)/dashboard/page.tsx
@src/app/api/folders/route.ts

<interfaces>
<!-- Key anchors the executor should use directly — no hunting. -->

Modal.tsx current accent line (causes VIS-01):
```tsx
// Line 69-72 — accent is absolutely positioned; parent card has no overflow-hidden
{!hideTopAccent && (
  <div className="absolute top-0 left-0 right-0 h-px bg-rs-gradient rounded-t-2xl opacity-60" />
)}
```
Fix: add `overflow-hidden` to the card container (line 63-68) so the accent line is clipped by `rounded-2xl`. Do NOT remove `rounded-t-2xl` on the accent (harmless either way once clipped).

AssetCard version badge (VIS-05):
```tsx
// Line 475-481 — overlay badge (KEEP this one)
<div className={`px-1.5 py-0.5 backdrop-blur-sm rounded text-xs text-white flex items-center gap-1 font-medium ${
  versionCount > 1 ? 'bg-frame-accent/80' : 'bg-black/60'
}`}>
  <Layers className="w-3 h-3" />
  V{versionCount}
</div>

// Line 619-621 — info row duplicate (REMOVE)
{versionCount > 1 && (
  <p className="text-xs text-frame-accent font-medium">{versionCount} versions</p>
)}
```

AssetCard thumbnail container (VIS-04):
```tsx
// Line 353 — aspect-video bg-black with object-cover children
<div className="relative aspect-video bg-black overflow-hidden">
  {/* image: <Image ... className="object-cover" /> */}
  {/* video thumbnailUrl: <img className="w-full h-full object-cover" /> */}
```
Fix: swap `object-cover` → `object-contain` on BOTH the image branch AND the video thumbnail/video-element branches. Keep `aspect-video bg-black` (letterbox behind). Do NOT touch the sprite-hover overlay (that's full-bleed by design).

AssetCard + FolderCard inline rename (VIS-03):
```tsx
// AssetCard line 592-609, FolderCard line 1611-1626 — both use:
onBlur={commitRename}  // REMOVE this
```
Both renderings follow the same shape — input + Enter/Escape handlers. Replace the single input with a flex row: `<input /> <button title="Confirm"><Check/></button> <button title="Cancel"><X/></button>`. Blur does nothing. Commit triggers on Enter or on check click. Cancel via Escape or X click.

AssetCard tag overlays (VIS-06):
```tsx
// Line 467-481 — top-left badges. Type badge uses bg-black/60 backdrop-blur-sm (good),
// but verify the review status badge (line 485-487) and duration badge (line 491-495).
// ReviewStatusBadge component may not have dark-bg treatment.
```
Fix: ensure the ReviewStatusBadge variant used over thumbnails has `bg-black/60 backdrop-blur-sm` wrapping OR already has dark-on-light contrast. If the component renders light text on a light-ish colored chip, wrap it in a `bg-black/50 backdrop-blur-sm rounded` container. Same for duration. Err on the side of readability; do NOT redesign the badge.

Dashboard Quick Actions (VIS-08):
```tsx
// Line 142-162 — all three hrefs are "/projects". Distinct destinations:
// - Browse Projects → "/projects" (keep)
// - Upload Assets   → "/projects?action=upload"   (projects list page reads ?action=upload and auto-opens the upload modal for the user to pick a project)
// - Invite Team     → "/projects?action=invite"   (same pattern — opens invite/collaborators flow)
```
NOTE: If the projects page does not yet read `?action=*`, add a minimal effect on the projects list page to handle the two actions (open the upload modal / open the invite modal). If that page is out of scope for this plan, fall back to:
- Upload Assets → `/projects` with query `?upload=1`
- Invite Team   → `/projects` with query `?invite=1`
and leave a TODO for wiring in a follow-up. The hard requirement is: each href is **distinct**.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1 (VIS-01): Clip Modal accent to card edge</name>
  <files>src/components/ui/Modal.tsx</files>
  <action>
    In `src/components/ui/Modal.tsx`, add `overflow-hidden` to the card container className (currently line 63-68). The accent line at line 69-72 is `absolute top-0 left-0 right-0 h-px bg-rs-gradient rounded-t-2xl opacity-60` — with the card now `overflow-hidden`, the rounded corners clip it.

    Do NOT remove `hideTopAccent`, `rounded-2xl`, `shadow-2xl`, `animate-slide-up` or any other class. Only ADD `overflow-hidden` to the existing class list.

    Caution: `overflow-hidden` on the Modal card can also clip nested dropdowns that open outward (e.g. date pickers). Check that no current usage renders a popover that escapes the card; if it does, use `overflow-x-hidden` instead so the horizontal accent still gets clipped but vertical dropdowns are unaffected. If both are required, use `[clip-path:inset(0_0_0_0_round_1rem)]` on the accent line itself instead of overflow on the card.

    Addresses D-01 (VIS-01).
  </action>
  <verify>
    <automated>npm run typecheck</automated>
    Visual: open any Modal instance (New Folder modal is the canonical case) and confirm the purple gradient line does not extend past the rounded corners.
  </verify>
  <done>
    The top accent line is visually contained within the rounded card; no pixel of the gradient extends beyond `rounded-2xl`. No dropdowns/popovers in existing modals are newly clipped.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2 (VIS-02a): Folder preview-assets API</name>
  <files>src/app/api/folders/[folderId]/preview-assets/route.ts</files>
  <action>
    Create a new GET route at `src/app/api/folders/[folderId]/preview-assets/route.ts`.

    Behavior:
    - Auth: mirror the auth pattern used by `src/app/api/folders/[folderId]/route.ts` (Bearer token → verifyIdToken → membership/permission check on the folder's projectId).
    - Query: list assets where `folderId == params.folderId` AND `deletedAt == null` (match existing asset listing filter used by `/api/folders`), order by `createdAt desc`, limit 4.
    - Shape: for each asset return `{ id, type, name, thumbnailSignedUrl?, signedUrl? }`. Signed URLs come from the same helper the grid uses (grep `thumbnailSignedUrl` in existing asset routes to find the signer). Only return URLs — do NOT return file metadata beyond what the tile needs.
    - Response: `{ assets: [...] }` with cache headers disabled (this is user-specific + short-lived URLs).

    Keep it ~50 lines. Do NOT add pagination, search, or any fields not listed above.

    Addresses D-02 (VIS-02).
  </action>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm run lint -- src/app/api/folders/[folderId]/preview-assets/route.ts</automated>
    Manual: `curl` against a dev server folder returns `{ assets: [...] }` with up to 4 entries, each with `thumbnailSignedUrl` or `signedUrl` when available.
  </verify>
  <done>
    Endpoint exists, returns up to 4 most-recent non-deleted assets with signed thumbnail URLs, gated by the same permissions as the existing folder endpoints.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3 (VIS-02b): FolderCard renders preview tiles</name>
  <files>src/components/files/FolderBrowser.tsx</files>
  <action>
    In `src/components/files/FolderBrowser.tsx`, modify the `FolderCard` component (starts line 1466) to fetch preview tiles on mount and render them above the folder name.

    Steps:
    1. Add a `useEffect` in FolderCard that calls `GET /api/folders/${folder.id}/preview-assets` with `Authorization: Bearer ${token}`. Store result in `const [preview, setPreview] = useState&lt;Array&lt;{id, type, thumbnailSignedUrl?, signedUrl?, name}&gt;&gt;([])`.
    2. Abort on unmount (AbortController) and on `folder.id` change.
    3. In the header row (line 1590-1610), replace the existing `<Folder className="w-8 h-8 ..." />` icon with a small preview container:
       - If `preview.length === 0` → render the existing `<Folder />` icon (unchanged fallback).
       - If `preview.length >= 1` → render a fixed-size tile container (e.g. `w-16 h-12 rounded overflow-hidden grid` with `grid-cols-1/2` depending on count):
         - 1 item: single tile
         - 2 items: 1×2 grid
         - 3–4 items: 2×2 grid (empty cells = `bg-frame-bg`)
       - Each tile: `<img src={thumbnailSignedUrl ?? signedUrl} className="w-full h-full object-cover" />`. For non-image/non-video assets without a thumbnail, render a type icon (lucide FileText/Film, size w-4 h-4, centered on `bg-frame-bg`).
    4. Do NOT change the rest of the card (rename input, dropdowns, context menu, drop targets). Only the icon slot changes.

    Keep the MoreHorizontal dropdown aligned to the top-right as today.

    Addresses D-02 (VIS-02).
  </action>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm run lint -- src/components/files/FolderBrowser.tsx</automated>
    Visual: folder with ≥1 asset shows tiled thumbnails; empty folder still shows the Folder icon. No layout shift in the folder grid.
  </verify>
  <done>
    FolderCard fetches `/api/folders/[id]/preview-assets` on mount and renders up to 4 tiled thumbnails; empty folders fall back to the existing folder icon; no other FolderCard behavior changes.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4 (VIS-03): Inline rename confirm buttons</name>
  <files>src/components/files/AssetCard.tsx, src/components/files/FolderBrowser.tsx</files>
  <action>
    Replace blur-to-commit rename with explicit check (commit) + X (cancel) buttons in both components.

    **AssetCard** (lines 592-609, the `{isRenaming ? (...) : (...)}` block in the info div):
    - Wrap the input in a flex row: `<div className="flex items-center gap-1">`.
    - Input: remove `onBlur={commitRename}`. Keep `onKeyDown` for Enter (commit) and Escape (cancel). Keep `onClick={(e) => e.stopPropagation()}`. Add `flex-1` to the input className.
    - Add two buttons after the input:
      - `<button type="button" title="Confirm" onClick={(e) => { e.stopPropagation(); commitRename(); }} className="p-1 rounded hover:bg-frame-accent/20 text-frame-accent"><Check className="w-3.5 h-3.5" /></button>`
      - `<button type="button" title="Cancel" onClick={(e) => { e.stopPropagation(); setIsRenaming(false); }} className="p-1 rounded hover:bg-frame-border text-frame-textMuted"><X className="w-3.5 h-3.5" /></button>`
    - Keep the Check / X icon imports (both already imported at top of file).

    **FolderBrowser FolderCard** (lines 1611-1626): apply the same pattern to the folder rename input. `commitFolderRename` is the commit handler. Check / X icons are already imported in FolderBrowser.

    Do NOT change `commitRename` / `commitFolderRename` behavior (name validation + API call). Only change the trigger surface: Enter or check-click commits; Escape or X-click cancels; blur does nothing.

    Addresses D-03 (VIS-03).
  </action>
  <verify>
    <automated>npm run typecheck</automated>
    Manual: start an asset rename, click outside the card → name does NOT change. Repeat: click the checkmark → name commits. Repeat: type a new name, press Escape or click X → name reverts. Same three tests on a folder rename.
  </verify>
  <done>
    Blur does not commit a rename in either AssetCard or FolderCard. Check button commits; X button cancels; Enter commits; Escape cancels. No regressions to the non-rename path (display mode truncated name).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 5 (VIS-04): Aspect-preserving asset thumbnails</name>
  <files>src/components/files/AssetCard.tsx</files>
  <action>
    In `src/components/files/AssetCard.tsx`, change `object-cover` → `object-contain` on the three media branches inside the `aspect-video bg-black overflow-hidden` thumbnail container (line 353):

    - Line ~363: `<Image ... className="object-cover" />` → `className="object-contain"`
    - Line ~368: `<img src={thumbnailUrl} ... className="w-full h-full object-cover" />` → `"w-full h-full object-contain"`
    - Line ~378: `<video ... className="w-full h-full object-cover" />` → `"w-full h-full object-contain"`

    Do NOT touch the sprite-hover overlay (line 415-423) — that uses backgroundSize/backgroundPosition with a 20-frame sprite strip and must keep its current behavior (full-bleed tiling).

    Do NOT touch the list-view thumbnail (AssetListView line 441, 443) — VIS-04 is about the grid asset CARD only.

    Keep `aspect-video` + `bg-black` so tall/narrow sources letterbox on black.

    Addresses D-04 (VIS-04).
  </action>
  <verify>
    <automated>npm run typecheck</automated>
    Visual: upload a tall portrait JPEG — the grid card shows the full image with letterbox padding, not a stretched/cropped frame. Video cards with horizontal thumbnails look unchanged.
  </verify>
  <done>
    Image and video thumbnails display full frame via `object-contain` within the `aspect-video` container. Hover scrub / sprite overlay still functions. No change to list view.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 6 (VIS-05): Single version count badge</name>
  <files>src/components/files/AssetCard.tsx</files>
  <action>
    In `src/components/files/AssetCard.tsx`, remove the duplicate "{versionCount} versions" label in the info row (line 619-621):

    ```tsx
    // DELETE these lines:
    {versionCount > 1 && (
      <p className="text-xs text-frame-accent font-medium">{versionCount} versions</p>
    )}
    ```

    Keep the overlay badge (line 475-481) which shows `V{versionCount}` with the Layers icon — that is the single canonical badge. Keep `commentCount` rendering in the info row (line 613-618) unchanged.

    Do NOT change `versionCount` derivation on line 65. Do NOT touch AssetListView (it has a separate Versions column, not a duplicate).

    Addresses D-05 (VIS-05).
  </action>
  <verify>
    <automated>npm run typecheck &amp;&amp; grep -n "versions</p>" src/components/files/AssetCard.tsx || true</automated>
    The grep should return empty (no "N versions" label left). Visual: asset with 3 versions shows "V3" badge once on the thumbnail and nothing version-related in the info row.
  </verify>
  <done>
    AssetCard renders the version count exactly once (overlay badge). Info row shows only filename, size, comment count, upload date.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 7 (VIS-06): Legible tag pills on thumbnails</name>
  <files>src/components/files/AssetCard.tsx</files>
  <action>
    Audit the thumbnail-overlay badges in AssetCard (lines 467-496) for contrast. Status: the type badge (line 467-474) and duration badge (line 491-495) already use `bg-black/60 backdrop-blur-sm` + white text — these are fine.

    The review status badge at line 484-488 renders `<ReviewStatusBadge status={asset.reviewStatus} />` directly without a dark wrapper. Wrap it so it's legible on bright thumbnails:

    ```tsx
    {asset.reviewStatus && (
      <div className="absolute bottom-2 left-2">
        <div className="bg-black/50 backdrop-blur-sm rounded px-0.5 py-0.5">
          <ReviewStatusBadge status={asset.reviewStatus} />
        </div>
      </div>
    )}
    ```

    Do NOT modify `ReviewStatusBadge` itself (used in other places like the viewer header and list-view row — those don't sit over thumbnails). Only wrap the grid-card usage.

    If, after visual check, `ReviewStatusBadge` already has a sufficiently dark chip, drop the wrapper — but default to wrapping for safety.

    Addresses D-06 (VIS-06).
  </action>
  <verify>
    <automated>npm run typecheck</automated>
    Visual: find an approved asset with a bright thumbnail (e.g. white/pale JPEG). The status pill is readable — dark background + light text visible against the image.
  </verify>
  <done>
    Review status badge on AssetCard sits on a dark translucent backdrop and is readable on any thumbnail. Other usages of ReviewStatusBadge (viewer header, list view) are unchanged.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 8 (VIS-07): Contain Create Review Link modal controls</name>
  <files>src/components/review/CreateReviewLinkModal.tsx</files>
  <action>
    In `src/components/review/CreateReviewLinkModal.tsx`, audit the form (line 122-269) for any child that can overflow the `Modal size="md"` card.

    Likely offenders:
    1. The "expires" segmented control (line 225-245): `grid grid-cols-4 gap-1.5` with button labels like "30 days" — at `max-w-md` it's tight. If labels wrap or overflow, reduce label padding to `px-1 py-1.5` and enforce `min-w-0` on the grid cells so text truncates cleanly rather than pushing the grid outside the card.
    2. Toggle rows (lines 133-221): long description text like "Viewers can leave comments and annotations" could overflow if a parent is narrow — add `min-w-0` to the left `<div>` holding text, and `flex-shrink-0` to the toggle button. The outer row already uses `flex items-center justify-between`; add `gap-3` to guarantee spacing.
    3. Created-link preview row (line 97-115): the URL container uses `flex-1 ... truncate` — confirm it works; it should already.

    Also ensure the `<form>` itself uses `max-w-full overflow-hidden` at the outermost level OR rely on Modal's newly-added `overflow-hidden` (from Task 1). If Task 1 adds `overflow-hidden` to Modal, most bleed issues self-heal — but still apply `min-w-0` to the toggle-row text wrappers and the expires-grid buttons so text doesn't cause layout to push past the card.

    Do NOT redesign the modal. Only adjust sizing / `min-w-0` / `flex-shrink-0` / grid cell behavior.

    Addresses D-07 (VIS-07).
  </action>
  <verify>
    <automated>npm run typecheck</automated>
    Visual: open Create Review Link modal on a folder. Every control (name input, four toggles, expires segmented buttons, password input, Cancel/Create buttons) is inside the rounded card boundary with no horizontal bleed. Test at viewport 1280px and 390px.
  </verify>
  <done>
    All controls in the Create Review Link modal render inside the modal card at md size across desktop and mobile viewports. No control clips at the card's rounded border.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 9 (VIS-08): Distinct Dashboard Quick Action routes</name>
  <files>src/app/(app)/dashboard/page.tsx</files>
  <action>
    In `src/app/(app)/dashboard/page.tsx` (line 142-162), make each QuickAction href distinct:

    - Browse Projects: `href="/projects"` (unchanged)
    - Upload Assets: `href="/projects?action=upload"`
    - Invite Team: `href="/projects?action=invite"`

    This is a lightweight solution: the href is distinct (satisfies VIS-08 success criterion) and the projects page can read `?action=` to open the relevant modal in a follow-up. Do NOT implement the projects-page query handling here — that's out of scope for this plan. The failing success criterion is "each action's `href`/`onClick` is distinct"; this fix directly addresses that.

    If a projects-page `?action=` handler is trivial to add in one place (e.g. the projects listing page already has a "New Project" flow triggered by `?create=1` per the empty-state Link on line 122), follow the same pattern for `upload` and `invite`. If the handlers don't already exist and would require non-trivial plumbing into project-scoped modals, leave a `// TODO(VIS-08): wire ?action=upload and ?action=invite in projects page` comment adjacent to the hrefs.

    Addresses D-08 (VIS-08).
  </action>
  <verify>
    <automated>npm run typecheck &amp;&amp; grep -c 'href="/projects"' src/app/\(app\)/dashboard/page.tsx</automated>
    Expect grep count of 1 (was 3). Manual: hover each Quick Action card and confirm distinct URLs in the status bar.
  </verify>
  <done>
    The three Quick Actions have three distinct hrefs. "Browse Projects" → `/projects`, "Upload Assets" → `/projects?action=upload`, "Invite Team" → `/projects?action=invite`.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 10: Human verify — all 8 VIS fixes</name>
  <files>n/a — verification only</files>
  <action>Human verifies all 8 VIS fixes in the running app per the steps below. No code changes in this task.</action>
  <verify>All 8 steps below pass visually.</verify>
  <done>User replies "approved"; if any regress, re-open the relevant task.</done>
  <what-built>
    All 8 visual polish fixes: modal accent clipping, folder thumbnail preview, rename confirm buttons, aspect-preserving asset thumbnails, single version badge, legible tag contrast, contained review-link modal, distinct Quick Actions.
  </what-built>
  <how-to-verify>
    1. Open "New Folder" modal → top accent line does not protrude past the rounded corners.
    2. Navigate into any project with assets → each folder card shows up to 4 tiled thumbnails (or folder icon if empty).
    3. Rename a folder: click outside → no change. Click check → commits. Escape or X → cancels. Repeat on an asset card.
    4. Upload a tall portrait JPEG to a folder → asset card letterboxes the full image (not cropped).
    5. Asset with versions > 1 → only ONE version badge visible on the card (overlay V3), nothing extra in the info row.
    6. Set an asset to "Approved" over a bright thumbnail → status badge is readable (dark backdrop).
    7. Open Create Review Link modal → all controls sit inside the rounded card border at 1280px and at ~390px mobile width.
    8. Dashboard → hover each Quick Action → status bar URLs are distinct (`/projects`, `/projects?action=upload`, `/projects?action=invite`).
  </how-to-verify>
  <resume-signal>Reply "approved" if all 8 pass, or list which ones regressed.</resume-signal>
</task>

</tasks>

<verification>
- `npm run typecheck` clean.
- `npm run lint` clean on touched files.
- 8 success criteria manually verified (Task 10 checkpoint).
</verification>

<success_criteria>
All 8 requirements pass:
1. VIS-01: New Folder modal accent clipped to card edge
2. VIS-02: Folder cards show 1-4 thumbnail tiles when non-empty
3. VIS-03: Inline rename has visible check + X; blur does not commit
4. VIS-04: Asset card thumbnails preserve aspect ratio via object-contain
5. VIS-05: Version count badge shown exactly once per card
6. VIS-06: Tag pills legible on any thumbnail
7. VIS-07: Create Review Link modal controls stay within card boundary
8. VIS-08: Dashboard Quick Actions route to distinct destinations
</success_criteria>

<output>
After completion, create `.planning/phases/53-visual-polish/53-01-SUMMARY.md` capturing the delta per VIS, any files touched that weren't in `files_modified`, and any follow-up TODOs (notably: wiring `?action=upload` / `?action=invite` on the projects page if left as TODO).
</output>
