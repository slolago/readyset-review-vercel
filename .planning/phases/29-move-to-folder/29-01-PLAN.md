---
phase: 29-move-to-folder
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/files/AssetCard.tsx
autonomous: false
requirements:
  - MOVE-01
must_haves:
  truths:
    - "Right-clicking an asset shows a 'Move to...' option in the context menu"
    - "The hover Dropdown ('...' menu) also shows a 'Move to' option"
    - "Clicking 'Move to' from either menu opens the folder picker modal"
    - "After confirming a destination, the asset disappears from the source folder and appears in the destination"
    - "If the asset belongs to a version group, all group members move together"
  artifacts:
    - path: "src/components/files/AssetCard.tsx"
      provides: "Move to item in both Dropdown and context menu"
      contains: "Move to"
  key_links:
    - from: "src/components/files/AssetCard.tsx"
      to: "src/components/files/FolderBrowser.tsx"
      via: "onRequestMove prop"
      pattern: "onRequestMove"
    - from: "src/components/files/FolderBrowser.tsx"
      to: "/api/assets/[assetId]"
      via: "handleMoveSelected -> PUT /api/assets/:id { folderId }"
      pattern: "handleMoveSelected"
---

<objective>
Add "Move to" to the AssetCard hover Dropdown menu so that asset relocation is available from both the right-click context menu and the hover "..." menu.

Purpose: The entire move pipeline (context menu item, folder picker modal, handleRequestMoveItem, handleMoveSelected, and the API batch-move endpoint) already exists. The only gap is that the hover Dropdown menu is missing the "Move to" item. This plan closes that gap and verifies the end-to-end flow.

Output: Updated AssetCard.tsx with "Move to" in the Dropdown items array. Build passes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-move-to-folder/29-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add "Move to" item to AssetCard Dropdown menu</name>
  <files>src/components/files/AssetCard.tsx</files>
  <read_first>
    - src/components/files/AssetCard.tsx (full file — understand Dropdown items array at ~line 327 and context menu at ~line 432)
    - src/components/files/FolderBrowser.tsx (confirm handleRequestMoveItem and onRequestMove wiring)
  </read_first>
  <action>
In `src/components/files/AssetCard.tsx`, add a "Move to" item to the Dropdown `items` array (the hover "..." menu, starting at ~line 327).

Insert the following item object AFTER the "Copy to" entry (~line 337) and BEFORE the "Duplicate" entry:

```typescript
{
  label: 'Move to',
  icon: <MoveIcon className="w-4 h-4" />,
  onClick: () => onRequestMove?.(),
},
```

`MoveIcon` is already imported at the top of the file (from lucide-react). `onRequestMove` is already destructured from props (line 35). No new imports or prop changes are needed.

This matches the exact pattern used in the context menu at ~line 432. No other files need changes — the full pipeline (onRequestMove -> FolderBrowser.handleRequestMoveItem -> MoveModal -> handleMoveSelected -> PUT /api/assets/:id) is already wired end-to-end.

Do NOT:
- Add any new state to AssetCard (move state lives in FolderBrowser)
- Modify FolderBrowser, AssetGrid, AssetListView, or the API route (all are already correct)
- Add a separate MoveModal inside AssetCard (FolderBrowser owns it)
  </action>
  <verify>
    <automated>cd C:/Users/Lola/Documents/frame && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - src/components/files/AssetCard.tsx Dropdown items array contains an object with `label: 'Move to'`
    - The "Move to" Dropdown item calls `onRequestMove?.()`
    - The "Move to" Dropdown item uses `MoveIcon` as its icon
    - `npm run build` exits 0 with no TypeScript errors
    - No new imports added (MoveIcon and onRequestMove already exist)
    - No new state variables added to AssetCard
  </acceptance_criteria>
  <done>The Dropdown hover menu includes "Move to" between "Copy to" and "Duplicate". Build passes. The existing move pipeline (FolderBrowser.handleRequestMoveItem -> MoveModal -> handleMoveSelected -> API batch move) handles the rest.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify move-to-folder end-to-end</name>
  <files>src/components/files/AssetCard.tsx</files>
  <action>Human verification of the end-to-end move flow. No code changes — this is a visual/functional check of the pipeline wired in Task 1.</action>
  <read_first>
    - src/components/files/AssetCard.tsx (verify the Dropdown now contains "Move to")
  </read_first>
  <what-built>Added "Move to" to the AssetCard hover Dropdown menu. The full move pipeline was already wired from prior phases — this completes the UI surface.</what-built>
  <how-to-verify>
    1. Run `npm run dev` and open the app in your browser
    2. Navigate to a folder with at least one asset
    3. Hover over an asset card and click the "..." menu — confirm "Move to" appears between "Copy to" and "Duplicate"
    4. Click "Move to" — confirm the folder picker modal opens
    5. Select a different folder and confirm — the asset should disappear from the current folder
    6. Navigate to the destination folder — the asset should appear there
    7. Right-click an asset — confirm "Move to" still works from the context menu (regression check)
    8. If you have a versioned asset (version stack), move it and confirm ALL version group members moved together to the destination folder
  </how-to-verify>
  <verify>
    <automated>cd C:/Users/Lola/Documents/frame && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - User confirms "Move to" appears in the hover Dropdown menu
    - User confirms folder picker modal opens on click
    - User confirms asset relocates to destination folder
    - User confirms version group members all move together (if applicable)
  </acceptance_criteria>
  <done>User has approved the end-to-end move flow from both the Dropdown and context menu.</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `npm run build` passes with no errors
- AssetCard.tsx Dropdown items array includes "Move to" with MoveIcon and onRequestMove callback
- Context menu "Move to" unchanged (regression)
- End-to-end: Dropdown "Move to" -> folder picker modal -> confirm -> asset relocates
- Version group: all siblings move atomically
</verification>

<success_criteria>
1. Both the hover Dropdown ("...") and the right-click context menu show "Move to"
2. Clicking "Move to" from either menu opens the folder picker modal
3. Confirming a move relocates the asset (and all version group members) to the destination folder
4. The asset disappears from the source folder view after move
5. `npm run build` passes
</success_criteria>

<output>
After completion, create `.planning/phases/29-move-to-folder/29-01-SUMMARY.md`
</output>
