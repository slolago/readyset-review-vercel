---
phase: 75-page-loading-and-server-components
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/Skeleton.tsx
  - src/app/(app)/projects/loading.tsx
  - src/app/(app)/projects/[projectId]/loading.tsx
  - src/app/(app)/projects/[projectId]/folders/[folderId]/loading.tsx
  - src/app/(app)/projects/[projectId]/folders/[folderId]/page.tsx
  - src/app/(app)/projects/[projectId]/trash/loading.tsx
  - src/app/(app)/admin/loading.tsx
  - src/components/ui/Avatar.tsx
  - src/components/ui/Badge.tsx
  - src/components/ui/Spinner.tsx
  - src/components/ui/Breadcrumb.tsx
  - src/components/ui/ReviewStatusBadge.tsx
  - src/components/ui/Button.tsx
  - src/components/viewer/FileTypeCard.tsx
  - src/components/review/ReviewHeader.tsx
  - src/components/comments/CommentTimestamp.tsx
  - src/components/projects/ProjectCard.tsx
  - src/app/(app)/admin/page.tsx
autonomous: true
requirements:
  - PERF-15
  - PERF-16
  - PERF-17

must_haves:
  truths:
    - "/projects shows a skeleton while fetching — no blank white frame"
    - "/projects/[id] shows a skeleton while fetching — no blank white frame"
    - "/projects/[id]/folders/[folderId] shows a skeleton while fetching — no blank white frame"
    - "/projects/[id]/trash shows a skeleton while fetching — no blank white frame"
    - "/admin shows a skeleton while fetching — no blank white frame"
    - "6–10 pure presentational components render without 'use client'"
    - "Clicking the Projects tab in admin is instant — data was already fetched on mount"
  artifacts:
    - path: "src/app/(app)/projects/loading.tsx"
      provides: "Projects list loading skeleton"
    - path: "src/app/(app)/projects/[projectId]/loading.tsx"
      provides: "Project root loading skeleton"
    - path: "src/app/(app)/projects/[projectId]/folders/[folderId]/loading.tsx"
      provides: "Nested folder loading skeleton (or sibling replacement for fallback={null})"
    - path: "src/app/(app)/projects/[projectId]/trash/loading.tsx"
      provides: "Trash route loading skeleton"
    - path: "src/app/(app)/admin/loading.tsx"
      provides: "Admin panel loading skeleton"
    - path: "src/app/(app)/admin/page.tsx"
      provides: "Admin page with parallel eager fetch of users + projects on mount"
  key_links:
    - from: "loading.tsx files"
      to: "Skeleton primitive (shared or inline)"
      via: "import or inline animate-pulse markup"
      pattern: "animate-pulse.*bg-neutral-800"
    - from: "src/app/(app)/admin/page.tsx useEffect"
      to: "fetchUsers() + fetchProjects()"
      via: "Promise.all on mount"
      pattern: "Promise\\.all.*fetchUsers.*fetchProjects|fetchUsers\\(\\);\\s*fetchProjects\\(\\)"
---

<objective>
Close out Phase 75 in a single autonomous wave: add route-level loading skeletons (PERF-15), flip trivial presentational components from Client to Server (PERF-16), and make the admin panel eagerly fetch both tabs on mount (PERF-17).

Purpose: Eliminate blank-white-frame perception on every major route, cut hydration cost for 6–10 frequently-rendered primitives, and remove the ~500ms blank state when switching to the admin Projects tab.

Output: 5 new loading.tsx files (or 4 + 1 in-page Suspense swap for the nested folder route), a minimal Skeleton primitive if none exists, 6–10 "use client" removals on pure presentational components, and a one-line fix in admin/page.tsx to fire both fetches in parallel on mount.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/75-page-loading-and-server-components/75-CONTEXT.md

<!-- Scout targets before editing. Do not assume contents — read first. -->
@src/app/(app)/projects/[projectId]/folders/[folderId]/page.tsx
@src/app/(app)/admin/page.tsx
@src/components/ui/Button.tsx
@src/components/projects/ProjectCard.tsx

<interfaces>
<!-- Expected conventions to honor (verified by Read before edits) -->

Skeleton style (existing in v1.8/v1.9 loading states):
```tsx
// Pattern: animate-pulse bg-neutral-800/50 rounded-md {sizing}
<div className="animate-pulse bg-neutral-800/50 rounded-md h-4 w-32" />
```

App Router loading.tsx contract:
- Default-exported React component, no props
- Auto-wrapped in <Suspense> by the framework
- Rendered while the matching page.tsx awaits

Server Component flip criteria (remove "use client" ONLY if ALL true):
- No useState / useReducer / useContext consumer
- No useEffect / useLayoutEffect / useRef
- No useRouter / usePathname / useSearchParams
- No onClick / onChange / onSubmit / onMouseEnter etc. in JSX
- No onClick / onChange etc. accepted as props (cross-boundary function props throw)
- No browser-only APIs (window, document, localStorage, navigator)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PERF-15 — Add loading.tsx skeletons for 5 routes</name>
  <files>
    src/components/ui/Skeleton.tsx (create IF missing),
    src/app/(app)/projects/loading.tsx (create),
    src/app/(app)/projects/[projectId]/loading.tsx (create),
    src/app/(app)/projects/[projectId]/folders/[folderId]/loading.tsx (create — see action for fallback={null} strategy),
    src/app/(app)/projects/[projectId]/folders/[folderId]/page.tsx (edit ONLY if replacing fallback={null} inline is more idiomatic than a sibling loading.tsx),
    src/app/(app)/projects/[projectId]/trash/loading.tsx (create),
    src/app/(app)/admin/loading.tsx (create)
  </files>
  <read_first>
    1. `src/components/ui/` — list directory. If `Skeleton.tsx` already exists, USE IT. Do not overwrite.
    2. `src/app/(app)/projects/[projectId]/folders/[folderId]/page.tsx` — look for `<Suspense fallback={null}>`. If present, decide: (a) swap `null` for `<FolderBrowserSkeleton />` inline, OR (b) add a sibling `loading.tsx` and leave the page unchanged. App Router uses `loading.tsx` automatically when a route segment suspends, so (b) is cleaner IF the Suspense boundary wraps the whole page body. If the Suspense wraps a narrower subtree (e.g., only FolderBrowser, while a header renders eagerly), prefer (a). Document the choice in the commit message.
    3. Any existing `loading.tsx` in the repo (grep `loading.tsx` under `src/app`) — match its style for consistency. v1.8/v1.9 established the `animate-pulse bg-neutral-800/50` pattern.
    4. `ProjectCard.tsx` to mirror its card dimensions in the /projects skeleton.
    5. Trash page layout (list vs grid) to mirror row heights in the trash skeleton.
  </read_first>
  <action>
    Per PERF-15. Create the following, each as a default-exported React component with no props. Each file should be small (~15–40 lines) — 3–6 placeholder rows/cards matching the target layout's structure. No shimmer effects, no inline SVG, no fancy animations — only `animate-pulse bg-neutral-800/50 rounded-md` sized to match the real content.

    1. **Shared primitive (only if missing):** `src/components/ui/Skeleton.tsx` — ~10 lines:
       ```tsx
       type Props = { className?: string };
       export function Skeleton({ className = '' }: Props) {
         return <div className={`animate-pulse bg-neutral-800/50 rounded-md ${className}`} />;
       }
       ```
       If the file already exists, import from it instead of creating your own.

    2. **`src/app/(app)/projects/loading.tsx`** — grid of 6 ProjectCard-sized skeletons. Match the grid column count and card aspect ratio used by the real /projects page.

    3. **`src/app/(app)/projects/[projectId]/loading.tsx`** — breadcrumb line (single narrow skeleton) + asset grid skeleton (6–8 cards). Header area if the real page has one.

    4. **`src/app/(app)/projects/[projectId]/folders/[folderId]/loading.tsx`** OR inline swap — see `read_first` step 2. Either:
       - (a) Create the sibling loading.tsx with a breadcrumb + folder-content skeleton, OR
       - (b) Edit the page's `<Suspense fallback={null}>` to `<Suspense fallback={<FolderBrowserSkeleton />}>` and define `FolderBrowserSkeleton` inline or in `src/components/projects/FolderBrowserSkeleton.tsx`.
       Pick whichever is idiomatic for how the existing Suspense is scoped. Do NOT do both.

    5. **`src/app/(app)/projects/[projectId]/trash/loading.tsx`** — list skeleton: ~6 rows matching the trash list row height.

    6. **`src/app/(app)/admin/loading.tsx`** — stats card row (3–4 small cards) + table skeleton (~8 rows).

    Use `Skeleton` from `src/components/ui/Skeleton.tsx` (or inline `animate-pulse bg-neutral-800/50` divs if the primitive doesn't exist and creating one feels over-engineered for the number of uses).

    Constraint per CLAUDE.md: surgical — touch only the files above. Do not "improve" adjacent code, do not add props to Skeleton beyond `className`, do not create a story/test for Skeleton.
  </action>
  <verify>
    <automated>
      npx tsc --noEmit &amp;&amp;
      ls src/app/\(app\)/projects/loading.tsx src/app/\(app\)/projects/\[projectId\]/loading.tsx src/app/\(app\)/projects/\[projectId\]/trash/loading.tsx src/app/\(app\)/admin/loading.tsx &amp;&amp;
      (ls src/app/\(app\)/projects/\[projectId\]/folders/\[folderId\]/loading.tsx || grep -l "FolderBrowserSkeleton\|fallback={&lt;.*Skeleton" src/app/\(app\)/projects/\[projectId\]/folders/\[folderId\]/page.tsx)
    </automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0
    - All 4 new loading.tsx files exist at the paths above
    - The nested-folder route EITHER has a new loading.tsx OR its page.tsx no longer contains `fallback={null}` (grep returns 0 matches inside that file)
    - Every skeleton file contains `animate-pulse` and `bg-neutral-800` (grep confirms)
    - Skeleton primitive used where it exists
  </acceptance_criteria>
  <done>
    All 5 routes have a visible skeleton during route transitions — no blank white frames. Nested-folder fallback={null} replaced or superseded. Typecheck clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: PERF-16 — Flip pure presentational components from Client to Server</name>
  <files>
    src/components/ui/Avatar.tsx,
    src/components/ui/Badge.tsx,
    src/components/ui/Spinner.tsx,
    src/components/ui/Breadcrumb.tsx,
    src/components/ui/ReviewStatusBadge.tsx,
    src/components/ui/Button.tsx,
    src/components/viewer/FileTypeCard.tsx,
    src/components/review/ReviewHeader.tsx,
    src/components/comments/CommentTimestamp.tsx,
    src/components/projects/ProjectCard.tsx
  </files>
  <read_first>
    Read EACH of the 10 candidate files in full before deciding. For each, check for ALL of:
    - `"use client"` directive on line 1
    - `useState`, `useReducer`, `useRef`, `useEffect`, `useLayoutEffect`, `useContext`, `useMemo`, `useCallback`, `useTransition`
    - `useRouter`, `usePathname`, `useSearchParams`, `useParams` (next/navigation)
    - `onClick`, `onChange`, `onSubmit`, `onMouseEnter`, `onKeyDown` etc. in JSX OR accepted as props in the component's type signature
    - `window`, `document`, `localStorage`, `navigator`, `matchMedia`
    - `forwardRef` usage (usually fine to keep, but confirm the ref isn't used for a client-only API)

    If ANY of the above is present, the component cannot flip — note why and skip.

    Per CONTEXT.md specifics: Button accepts onClick as a prop → DO NOT flip. ProjectCard uses hover state → if the flip requires extracting a client wrapper, SKIP and document.
  </read_first>
  <action>
    Per PERF-16. For each of the 10 candidates, after scouting:

    - If the file qualifies (no hooks, no event handlers, no event-handler props, no browser APIs) → **delete the `"use client"` directive from line 1** (and remove the blank line it leaves behind only if adjacent import style dictates). Change nothing else in the file.
    - If the file does NOT qualify → **leave it unchanged**. Record the blocker.

    Target: 6–10 flips. The CONTEXT.md hints that Button and ProjectCard are likely skip candidates; the other 8 are likely flippable. Do not force-flip by refactoring — a flip is strictly the removal of the directive.

    After the scout + flips, record in the commit message body (and summary) a small table:

    | Component | Flipped? | Reason if skipped |
    |-----------|----------|-------------------|
    | Avatar | yes | — |
    | Badge | yes | — |
    | Button | no | accepts onClick prop |
    | ... | ... | ... |

    Constraint per CLAUDE.md: surgical. Do not rename, do not change prop types, do not add JSDoc, do not reformat. Only remove the directive in files that qualify.
  </action>
  <verify>
    <automated>
      npx tsc --noEmit &amp;&amp;
      npm run build 2&gt;&amp;1 | tee /tmp/75-01-build.log &amp;&amp;
      ! grep -E "You're importing a component that needs|cannot be used as a child of a server component.*that relies on" /tmp/75-01-build.log
    </automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0
    - `npm run build` exits 0 with no new "You're importing a component that needs..." warnings
    - Between 6 and 10 of the 10 candidate files no longer have `"use client"` on line 1 (grep the 10 files; count results should drop by 6–10 vs baseline)
    - Skipped files are documented in the commit body with reason
    - No component's public API (exported type signature) changed
  </acceptance_criteria>
  <done>
    6–10 presentational primitives render as Server Components. Build passes with no new RSC boundary warnings. Skipped files documented.
  </done>
</task>

<task type="auto">
  <name>Task 3: PERF-17 — Admin panel eager parallel fetch on mount</name>
  <files>src/app/(app)/admin/page.tsx</files>
  <read_first>
    1. `src/app/(app)/admin/page.tsx` — full file. Locate:
       - The mount `useEffect` that currently calls `fetchUsers()` only (CONTEXT says lines 34–49).
       - The tab-click handler that calls `fetchProjects()` on first Projects-tab click (CONTEXT says lines 69–77).
       - The `fetchUsers` / `fetchProjects` definitions (or imports) to confirm they return Promises and don't require arguments in the call sites.
    2. Confirm the existing error state handling for each table — do not regress it. If each fetch independently sets an error state, `Promise.all` will reject on first failure; use either `Promise.allSettled` or two concurrent `.catch()`ed calls so one failure doesn't blank the other table.
  </read_first>
  <action>
    Per PERF-17. Make the following minimal edits to `src/app/(app)/admin/page.tsx`:

    1. In the mount `useEffect`, fire both fetches concurrently. Preferred shape (independent failure):
       ```ts
       useEffect(() =&gt; {
         fetchUsers();
         fetchProjects();
       }, []);
       ```
       OR if the existing code awaits the mount fetch and needs sequencing control:
       ```ts
       useEffect(() =&gt; {
         Promise.allSettled([fetchUsers(), fetchProjects()]);
       }, []);
       ```
       Pick whichever matches the existing code's idiom. Both fetches must start in the same tick.

    2. Remove the tab-click-triggered `fetchProjects()` call (CONTEXT says around lines 69–77). The Projects tab becomes a pure UI toggle — data is already loaded (or loading) from mount.

    3. Preserve any loading / error state each fetch manages. If a fetch is in-flight when the user clicks the Projects tab, the table should render its existing loading state (not a blank state). If `fetchProjects` currently guards against double-fetching (e.g., early-returns if data already present), that guard is now unreachable via the tab-click path but should stay — harmless, and the mount call benefits from it if the effect were ever to run twice (React 18 strict mode).

    Constraint per CLAUDE.md: surgical. Do not refactor the `useEffect` dependency array, do not rename `fetchUsers`/`fetchProjects`, do not restructure the tab component. Just concurrent fire on mount + remove the on-click fetch call.
  </action>
  <verify>
    <automated>
      npx tsc --noEmit &amp;&amp;
      grep -E "fetchUsers\(\);[[:space:]]*fetchProjects\(\)|Promise\.(all|allSettled)\(\s*\[\s*fetchUsers\(\)\s*,\s*fetchProjects\(\)" "src/app/(app)/admin/page.tsx" &amp;&amp;
      ! grep -n "fetchProjects()" "src/app/(app)/admin/page.tsx" | grep -Ev "useEffect|Promise\.(all|allSettled)|const fetchProjects|function fetchProjects|^[[:space:]]*//|^[[:space:]]*\*"
    </automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0
    - The mount `useEffect` in `admin/page.tsx` calls BOTH `fetchUsers()` and `fetchProjects()` in the same tick (grep confirms the concurrent pattern)
    - No `fetchProjects()` invocation remains inside a tab-click handler (grep shows the only call sites are the mount effect and the function definition)
    - Existing error + loading state for each table still wires through unchanged
  </acceptance_criteria>
  <done>
    Admin page fetches users + projects in parallel on mount. Projects tab click is instant. Typecheck clean.
  </done>
</task>

</tasks>

<verification>
Phase-level checks after all 3 tasks land:

1. `npx tsc --noEmit` exits 0
2. `npm test` passes (expected 171/171 — baseline from prior phase)
3. `npm run build` exits 0 with no new RSC boundary warnings
4. Grep `loading.tsx` under `src/app/(app)/` returns the expected files for all 5 routes (or 4 + the nested-folder in-page skeleton swap)
5. Grep `"use client"` across the 10 PERF-16 candidate files returns 0–4 hits (6–10 flipped)
6. `admin/page.tsx` mount effect calls both fetches; no tab-click fetch call remains
</verification>

<success_criteria>
From ROADMAP Phase 75:
1. `/projects`, `/projects/[id]`, `/projects/[id]/folders/[folderId]`, `/projects/[id]/trash`, and `/admin` all show a skeleton while fetching — no blank white frames
2. Avatar / Badge / Spinner / Breadcrumb / FileTypeCard / ReviewHeader / CommentTimestamp / ReviewStatusBadge / Button / ProjectCard shell render without `"use client"` (target 6–10; Button and ProjectCard may be skipped per documented reasons)
3. Clicking the Projects tab in admin is instant — data was already fetched on mount

Measurable:
- 5 loading.tsx files (or 4 + 1 inline skeleton swap) exist
- 6–10 "use client" directives removed across the 10 candidate files
- `admin/page.tsx` mount useEffect contains both `fetchUsers()` and `fetchProjects()` in the same tick; no tab-click fetch remains
- `npx tsc --noEmit` + `npm test` (171/171) + `npm run build` all pass
</success_criteria>

<output>
After completion, create `.planning/phases/75-page-loading-and-server-components/75-01-SUMMARY.md` documenting:
- Which skeleton strategy won for the nested-folder route (new loading.tsx vs in-page fallback swap), and why
- Whether a shared Skeleton primitive was created or one already existed
- The flip table from Task 2 (Component | Flipped? | Reason if skipped)
- Final count of flipped components vs skipped (confirm 6–10 flipped)
- Any surprise blockers that came out of scouting (e.g., a component that looked flippable but had a hidden client-only dep)
- Explicit deferrals carried to future phases: ProjectCard hover-client-wrapper refactor (NOT attempted per CONTEXT), `/review/[token]` page (Phase 76), FolderBrowser internals (Phase 77)
</output>
