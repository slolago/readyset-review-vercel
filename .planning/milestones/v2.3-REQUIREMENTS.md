# Requirements Archive: v2.3 App-Wide Performance Polish

**Shipped:** 2026-04-22
**Source:** 4-stream parallel app-wide perf audit (pages, viewer/player, data layer, bundle)

## Requirements (all complete)

### Viewer critical path

- [x] **PERF-10** → Phase 74: `<video>` uses `preload="metadata"` instead of `"auto"`
- [x] **PERF-11** → Phase 74: `<video>` sets `poster={asset.thumbnailUrl}`
- [x] **PERF-12** → Phase 74: `fabric.js` pre-warmed via fire-and-forget dynamic import on viewer mount
- [x] **PERF-13** → Phase 74: `VUMeter` + `AudioContext` initialize on first play, not viewer mount
- [x] **PERF-14** → Phase 74: Asset viewer renders `<VideoPlayer>` as soon as asset resolves; comments load in Suspense boundary

### Page loading + Server Components

- [x] **PERF-15** → Phase 75: `loading.tsx` skeletons for `/projects`, `/projects/[id]`, `/projects/[id]/folders/[folderId]`, `/projects/[id]/trash`, `/admin`
- [x] **PERF-16** → Phase 75: Shipped — Badge, Spinner, ReviewHeader, ReviewStatusBadge (pre-existing SC). Deferred — Avatar, Breadcrumb, Button, FileTypeCard, CommentTimestamp, ProjectCard (all require client-wrapper extraction)
- [x] **PERF-17** → Phase 75: Admin panel parallel-fetches users + projects on mount

### Asset viewer restructure

- [x] **PERF-18** → Phase 76: 5 heavy modals `next/dynamic`-imported with `ssr: false` + ModalSkeleton fallback
- [x] **PERF-19** → Phase 76: `useComments.addComment` optimistic insert with tempId + reconciliation + rollback
- [x] **PERF-20** → Phase 76: `AnnotationCanvas` read-only mount guard; ExportModal preview `<video>` deferred via `if (!open) return null`
- [x] **PERF-21** → Phase 76: `VersionComparison` dual-player mount uses stable `compare-A-${id}` / `compare-B-${id}` keys

### Folder browser decomposition

- [x] **PERF-22** → Phase 77: `useProject` mount uses `Promise.all([fetchProject(), fetchFolders(null)])`
- [x] **PERF-23** → Phase 77: AssetGrid/AssetListView already memoized; fixed inline callbacks defeating memo + narrowed `RenameProvider` scope to content div

### Data layer + bundle + network

- [x] **PERF-24** → Phase 78: `.limit(N).startAfter(cursor)` + `nextCursor` on `/api/admin/users`, `/api/admin/projects`, `/api/review-links/all`
- [x] **PERF-25** → Phase 78: Composite index `comments(assetId, reviewLinkId)`; `db.getAll` folder batch; chunk-20 signed-URL fan-out; `/api/assets` Cache-Control header
- [x] **PERF-26** → Phase 78: `next/font/google` Inter in layout.tsx; `modularizeImports` for lucide-react in next.config.mjs
- [x] **PERF-27** → Phase 78: Preconnect hints for firestore+storage; 2 raw `<img>` → `next/image` in AssetListView; `date-fns` removed from deps (zero uses in src/)

## Traceability

| Requirement | Phase | Status |
|-------------|-------|--------|
| PERF-10..14 | Phase 74 | Complete (Verification: passed) |
| PERF-15..17 | Phase 75 | Complete (Verification: human_needed — SC flip count shortfall accepted) |
| PERF-18..21 | Phase 76 | Complete (Verification: human_needed — 2 runtime UX spot-checks) |
| PERF-22..23 | Phase 77 | Complete (Verification: passed) |
| PERF-24..27 | Phase 78 | Complete (Verification: passed) |

**Coverage:** 18/18 requirements shipped. 2 phases passed cleanly; 3 phases flagged `human_needed` for runtime UX checks (structural code verified).

**Operational follow-up:** `firebase deploy --only firestore:indexes` activates the new comments composite index. Existing in-memory fallback keeps comments working until deployed.

---
*Archived: 2026-04-22*
